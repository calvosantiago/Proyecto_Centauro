# Core dependencies
python-docx
rapidfuzz
python-dotenv
chromadb
openai>=1.0.0
pydantic>=2.0.0
fpdf
scikit-learn
pandas
numpy
# pyannote.audio (solo si vas a meterte con audio puro)
# Chainlit para interfaz web
chainlit>=1.0.0

# Lectura de metadatos de audio (duración MP3 para cálculo de costes Whisper)
mutagen

# Transcripción y diarización de audio (AssemblyAI — activo)
assemblyai

# Transcripción de audio con Groq Whisper (desactivado del flujo principal, mantenido como fallback)
groq

# Análisis acústico de audio (métricas objetivas para EstiloAgent)
librosa

# Base de datos (Supabase - PostgreSQL hosted)
supabase

# Autenticación Microsoft (Power BI / Fabric — Device Code Flow)
msal

# Gráficos automáticos en respuestas Power BI
matplotlib

# Optional (mejora performance)
uvicorn[standard]
