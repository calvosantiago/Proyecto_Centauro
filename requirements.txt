# Core dependencies
python-docx
rapidfuzz
python-dotenv
chromadb
openai>=1.0.0
pydantic>=2.0.0
fpdf
scikit-learn
pandas
numpy
# pyannote.audio (solo si vas a meterte con audio puro)
# Chainlit para interfaz web
chainlit>=1.0.0

# Lectura de metadatos de audio (duración MP3 para cálculo de costes Whisper)
mutagen

# Optional (mejora performance)
uvicorn[standard]
